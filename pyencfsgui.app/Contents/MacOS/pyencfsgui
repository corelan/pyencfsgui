#!/bin/zsh

set -e

BUNDLE_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
REPO_DIR="$(cd "$BUNDLE_ROOT/.." && pwd)"

cd "$REPO_DIR"
nohup "$REPO_DIR/pyencfsgui.sh" >/tmp/pyencfsgui-launcher.log 2>&1 &
exit 0
